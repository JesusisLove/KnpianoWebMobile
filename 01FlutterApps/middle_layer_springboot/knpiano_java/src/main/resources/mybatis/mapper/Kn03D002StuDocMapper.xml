<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liu.springboot04web.mapper.Kn03D002StuDocMapper">

    <select id="getInfoList" resultType="com.liu.springboot04web.bean.Kn03D002StuDocBean">
        SELECT * FROM v_info_student_document
    </select>

    <select id="searchStuDoc" resultType="com.liu.springboot04web.bean.Kn03D002StuDocBean">
        SELECT *
        FROM v_info_student_document
        <where>
            <foreach item="value" index="key" collection="params">
                <if test="value != null and value != ''">
                    AND ${key} like CONCAT('%', #{value}, '%')
                </if>
            </foreach>
        </where>
    </select> 

    <select id="getInfoByKey" resultType="com.liu.springboot04web.bean.Kn03D002StuDocBean">
        SELECT * FROM v_info_student_document WHERE stu_id = #{stuId} 
                                           AND subject_id = #{subjectId} 
                                           AND adjusted_date = #{adjustedDate}
    </select>

    <!-- 从学生档案表视图中取得在课学生正在上的科目，在新规画面实现学生与科目的下拉列表框的联动：科目信息取决于选择的学生 -->
    <select id="getLatestSubjectList" resultType="com.liu.springboot04web.bean.Kn03D002StuDocBean">
        SELECT * FROM v_latest_subject_info_from_student_document order by stu_name
    </select>
    
    <insert id="insertInfo" parameterType="com.liu.springboot04web.bean.Kn03D002StuDocBean">
        insert into t_info_student_document (
            stu_id,
            subject_id,
            adjusted_date,
            pay_style,
            minutes_per_lsn,
            lesson_fee,
            lesson_fee_adjusted,
            exam_date,
            exam_score
            )
        values (
            #{stuId},
            #{subjectId},
            #{adjustedDate},
            #{payStyle},
            #{minutesPerLsn},
            #{lessonFee},
            #{lessonFeeAdjusted},
            #{examDate},
            #{examDcore}
        )
    </insert>

    <update id="updateInfo" parameterType="com.liu.springboot04web.bean.Kn03D002StuDocBean">
            update t_info_student_document set
                stu_id = #{stuId},
                subject_id = #{subjectId},
                adjusted_date = #{adjustedDate},
                pay_style = #{payStyle},
                minutes_per_lsn = #{minutesPerLsn},
                lesson_fee = #{lessonFee},
                lesson_fee_adjusted = #{lessonFeeAdjusted},
                exam_date = #{examDate},
                exam_score = #{examDcore},
                del_flg = #{delFlg}
            WHERE stu_id = #{stuId}
              AND subject_id = #{subjectId}
              AND adjusted_date = #{adjustedDate}
    </update>

    <delete id="deleteInfoByKeys">
        DELETE FROM t_info_student_document WHERE stu_id = #{stuId} 
                                         AND subject_id = #{subjectId} 
                                         AND adjusted_date = #{adjustedDate}
    </delete>

    <select id="getNextSequence" parameterType="map" statementType="CALLABLE">
        {
            #{parm_out,mode=OUT,jdbcType=INTEGER} = call nextval(#{parm_in,mode=IN})
        }
    </select>

    <select id="getLsnPrice" resultType="com.liu.springboot04web.bean.Kn03D002StuDocBean">
        SELECT pay_style, adjusted_date, minutes_per_lsn, lesson_fee, lesson_fee_adjusted
        FROM KNStudent.t_info_student_document 
        WHERE stu_id = #{stuId} 
            AND subject_id = #{subjectId} 
        ORDER BY adjusted_date DESC
        LIMIT 1;
    </select>

</mapper>
